Files here:

  - mintwin.m has magma code for computing the minimal sibling for a polynomial
  - mod3-prep.m has magma code for processing GL(2,3) representations
  - prep-mod2.gp has gp code for processing GL(2,2) representations
  - prep-sp4.gp has gp code for processing Sp(4,2) representations
  - topslope.gp has gp code for computing the top slope given a polynomial
    and a prime
